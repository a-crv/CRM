import mapping from './mappingDataToTable';
import onGetDataSuccess from './findLinkOnNextBatch';

export {
  mapping,
  onGetDataSuccess
};

const Helpers = {};

export default Object.assign(Helpers, {
  mapping,
  onGetDataSuccess
});
